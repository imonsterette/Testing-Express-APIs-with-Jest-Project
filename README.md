# Testing Express APIs with Jest — Project

Scaffolded for an Express API tested with Jest + Supertest.

## Scripts

- `npm test` — run tests
- `npm run test:watch` — watch mode
- `npm start` — start the server (will use server.js later)

## Tech

Express, Jest, Supertest
